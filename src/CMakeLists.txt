# src/CMakeLists.txt
# Этот файл не используется системой сборки.
# Все библиотеки объявлены в cmake/include.cmake
# и подключаются через app/CMakeLists.txt.
# Файл оставлен пустым чтобы не мешать сборке.